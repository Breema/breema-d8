id: breema_csv_shortcut_set
label: CSV Shortcut sets
migration_tags:
  - CSV
  - Breema CSV
  - Configuration

destination:
  plugin: entity:shortcut_set

source:
  plugin: breema_migrate_csv
  path: shortcut-set.csv
  header_row_count: 1
  keys:
    - id

process:
  id: id
  label: label

migration_dependencies:
  required: { }
