{# 
/**
 * Re-use the template with location at the end, since it'll be empty.
 * We want everything else (date above the title, etc) shared.
 */
#}
{% extends 'node--event--teaser-with-location.html.twig' %}
